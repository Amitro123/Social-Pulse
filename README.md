    ```bash
    pip install -r requirements.txt
    ```
2.  Copy `.env.example` to `.env` and fill in your credentials.
3.  Run the collector/analyzer:
    ```bash
    python src/main.py
    ```

## Project Structure

See `docs/spec.md` for detailed architecture.
